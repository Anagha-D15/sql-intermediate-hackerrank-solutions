WITH CTE AS(
    SELECT LAT_N,
           ROW_NUMBER() OVER (ORDER BY LAT_N) AS rn,
           COUNT(*) OVER () AS cnt
    FROM STATION )
SELECT ROUND(AVG(LAT_N), 4) AS median
FROM CTE
WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2);


